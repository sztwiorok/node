import assert from 'assert';
import tddTest from '../lib';

describe('tdd-test', function () {
  it('2nd test', function () {
    assert(true, 1===1);
  });
  it('2nd test', function () {
     assert(true, 1===1);
   });
});
